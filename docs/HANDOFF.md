# Session handoff — Fanta Formazioni Bot

> Scritto il 2026-07-07 al termine della sessione di riscrittura v2, riconciliato allo stato corrente il 2026-07-14. Destinatario: la prossima sessione di Claude Code (e Matteo). Aggiornarlo (o snellirlo) alla fine delle sessioni significative; per i fatti volatili (PR aperte, versione in prod) verificare sempre lo stato reale con `gh`.

## 1. Documenti essenziali da leggere (in quest'ordine)

1. **`CLAUDE.md`** (root) — comandi, struttura, invarianti, git workflow. Sempre per primo.
2. **`docs/ARCHITECTURE.md`** — componenti, flussi (startup / refresh / reminder), schema DB, tabella env vars.
3. **`docs/adr/0001–0024`** — una ADR per decisione architetturale. **Leggere la ADR pertinente prima di toccare una scelta architetturale; scrivere una nuova ADR quando se ne prende una.** Le più consultate: 0005 (scheduling esatto), 0008 (+ amendment: subscription `origin`, già pensata per i canali user-owned), 0010 (dev/prod environments, amendata da 0017), 0012 (comandi subscription, pattern admin-only da riusare), 0015 (tastiere inline: bitmask stateless in callback_data, ConversationHandler per input libero), 0016 (provider `mock` per test dei reminder pre-stagione, solo dev), **0017 (trunk-based + release via tag, sostituisce ADR 0011 — leggerla per il git workflow attuale)**, **0018 (voce & tono: lessico, convenzione emoji, guardrail "informazione prima della battuta" — riferimento obbligato per qualsiasi nuovo testo utente, + Amendment 2026-07-10 con le regole su comandi legacy/promossi e liste bullet)**, **0019 (fix del bug "Salva sovrascrive i custom" in `/personalizza_orari`, flusso Personalizzati→submit→Salva sulla griglia riaperta)**, **0020 (Salva ora merge-safe: chiude il limite residuo lasciato aperto da 0019 — leggerla prima di toccare `save_offsets_callback` o la logica mask in `telegram/keyboards.py`)**, **0021 ("Ho schierato" per chat private: perché il gruppo/canale è stato escluso — chiave di lettura obbligata prima di estendere la feature ai gruppi, vedi sezione 3 sotto)**. ⚠️ **ADR 0011 è superseded**: non seguirla per il flusso di rilascio.
4. **`docs/DEPLOY.md`** — VM Oracle, environments GitHub, workflow CI/CD, convenzioni di merge, operations.

## 2. Cosa è stato fatto e perché

**Riscrittura completa (v1 → v2, rilasciata come 0.9.0, oggi in prod v0.12.2).** La v1 (Poetry, PTB 21, polling ogni 5s, datetime naive, locale di sistema, zero test/CI) è stata sostituita da zero: uv+ruff+mypy strict+pytest, PTB ~22.8, job `run_once` a orari esatti con dedupe su SQLite, kickoff "pulito" + `DEADLINE_MARGIN` configurabile, provider calendario pluggabile (fixturedownload CSV UTC), testi italiani in HTML parse mode. Motivazioni dettagliate nelle ADR 0001–0009.

**Infrastruttura (tutta funzionante e verificata):**
- VM **Oracle Cloud Always Free** `VM.Standard.E2.1.Micro` (x86, 1GB RAM + 2GB swap), IP `130.110.70.70`, utente `ubuntu`. SSH: alias `fantabot` in `~/.ssh/config` (chiave `~/.ssh/oracle_fantabot`); chiave di deploy per Actions: `~/.ssh/fantabot_deploy` (privata nel secret `SSH_KEY`).
- Era prevista la A1.Flex (ARM) ma è sempre out-of-capacity: per questo l'immagine è **multi-arch** (amd64+arm64). Migrare ad A1 quando c'è capacità è banale (stessa procedura DEPLOY.md).
- **Due istanze sulla stessa VM**: prod (`~/fantaformazionibot`, immagine `:latest`, canale @fantaformazionireminders id `-1002189068048`, deploy da **push di un tag `v*`**) e dev (`~/fantaformazionibot-dev`, immagine `:dev`, debug chat `-1002171697436`, deploy da **ogni push su `main`**). Due bot Telegram distinti (il polling vieta token condivisi). Mapping aggiornato da ADR 0017 (prima: push su `main`/`dev`).
- **Config gestita dalla pipeline** (ADR 0010): il deploy riscrive `.env` e `compose.yaml` sulla VM dai secrets/vars degli environments GitHub `production`/`development`. MAI modificarli a mano sulla VM. Cambio config = aggiornare il valore su GitHub → rilanciare Deploy.
- Il bot **dev risponde solo a Matteo** (user id `41755391`) e alla debug chat, via `ALLOWED_CHAT_IDS` (vuota in prod = aperto).

**Flusso di lavoro (ADR 0017 — trunk-based + release via tag, sostituisce ADR 0011):**
- `main` = trunk e default branch; feature → PR su `main` → **squash merge** → deploy automatico del bot dev (ogni push su `main`).
- Release = **tag git `vX.Y.Z`**, non più una PR: `git tag vX.Y.Z && git push origin vX.Y.Z` → deploy prod + GitHub Release automatici in `deploy.yml`. Niente più workflow "Prepare release", niente bump commit, niente versione in `pyproject.toml` (il tag è l'unica fonte di verità).
- Al **1.0.0**: tag su `main` + cut del branch long-running **`release-1.0`** (solo fix stagionali, patch `v1.0.x`). Fix: **prima su `main`**, poi cherry-pick su `release-1.0` + tag patch.
- `main` ha branch protection (1 review); "Automatically delete head branches" attivo. `release-1.0`, una volta creato, va protetto e aggiunto a `ci.yml`.
- Fix di sicurezza: token dei workflow a minimo privilegio (alert CodeQL risolti).
- **Migrazione da fare (manuale, vedi ADR 0017 § runbook)**: reset di `main` sul tip di `dev` (droppa i merge commit vuoti), impostare `main` come default branch su GitHub, cancellare `dev`.

**Bug notevoli risolti (da conoscere):**
- Cambiare `CHANNEL_CHAT_ID` lasciava la vecchia subscription nel DB (reminder duplicati a entrambe le chat). Fix: colonna `origin` (`env`/`user`) + pruning all'avvio delle sole righe channel `origin='env'` stale. Le future subscription utente (`origin='user'`) non vengono mai toccate.
- Docker: `uv sync` senza `--no-editable` produce un venv che punta a `/app/src` inesistente nello stage finale.

**Aggiornamento 2026-08-27 — errori di consegna e ciclo di vita delle subscription:**
- PR #30 (mergiata su `main`, `7a91dbc`): `unknown_command` ignora i comandi indirizzati ad altri bot (`/list@altro_bot`), con match dello username case-insensitive come fa `CommandHandler` di PTB. In più `errors.py` classifica gli errori di destinazione non scrivibile e non li riporta più sul debug chat: erano rumore su cui nessuno può agire.
- ⚠️ **Il rilascio è `v1.1.0` su `main`, non `v1.0.3`**: la #31 porta con sé una tabella nuova e un contratto con osservatorio-hq, quindi non è una patch (vedi emendamento ADR 0017). `release-1.0` è ritirata; su di essa resta `fa070dd` (cherry-pick della #30) mai taggato, innocuo. La linea di manutenzione in stagione è **`release-1.1`**, tagliata al tag. Verificare sempre con `git ls-remote --tags origin` prima di assumere che qualcosa sia in prod.
- **ADR 0023**: una chat che blocca/caccia il bot veniva mantenuta in `subscriptions` e generava reminder falliti per tutta la stagione, in silenzio. Ora `send_reminder_job` distingue errori terminali (`is_dead_chat_error`) da transitori (topic chiuso) e sui primi pota la subscription + cancella i job pendenti di quella chat. La riga del canale `origin='env'` fa eccezione: si tiene e si segnala sul debug chat, perché `_post_init` la ricrea comunque a ogni riavvio.
- **ADR 0024**: nuova tabella append-only `subscription_events` (`subscribed` / `unsubscribed` / `dead_chat`) per *osservatorio-hq*. Serve a distinguere tre stati che prima collassavano nel nulla: attivo, iscritto-ma-non-attivo (`/promemoria_off`), cancellato (chat irraggiungibile). **Nessun read path del bot cambia**: `subscriptions` continua a significare esattamente "chi riceve i reminder". I nomi dei tre eventi sono un contratto con l'altro repo — aggiungerne di nuovi è sicuro, rinominarli no.

## 3. Cosa c'è da fare e come

**Roadmap funzionale (in ordine di priorità espressa da Matteo) — punti 1–3 in prod da v0.11.0, punto 4 da v0.12.0:**
1. ✅ **Promemoria privati per utente e per gruppo** (PR #15): `/promemoria_on`, `/promemoria_off`, `/promemoria`; nei gruppi on/off sono solo per admin. Decisioni in **ADR 0012**.
2. ✅ **Orari di notifica personalizzabili per subscription** (PR #16): `/personalizza_orari [offset...|default]` aggiorna `reminder_offsets` sulla riga della chat corrente (auto-iscrive se assente); senza argomenti mostra gli orari attuali. Validazione: unità s/m/h, 1 minuto–7 giorni, max 10 offset. Admin-only nei gruppi come `/promemoria_on`. Decisioni in **ADR 0013**.
3. ✅ **Provider football-data.org alternativo a fixturedownload + alert di staleness** (PR #17): `CALENDAR_PROVIDER` è ora un enum (`fixturedownload`/`football-data-org`, valori invariati per compatibilità); il secondo provider si attiva impostando `CALENDAR_PROVIDER=football-data-org` + `FOOTBALL_DATA_API_KEY` (switch manuale, non automatico). Il refresh giornaliero avvisa su `DEBUG_CHAT_ID` se la prossima giornata ha ancora un kickoff placeholder (`00:00` UTC) a meno di 3 giorni dalla scadenza. Decisioni in **ADR 0014**.

4. ✅ **Inline keyboard (bottoni callback)** (PR #19, in prod da v0.12.0): `/start`, `/promemoria` e `/personalizza_orari` (senza argomenti) hanno bottoni sopra la stessa logica di subscribe/unsubscribe/set-offsets; griglia di 8 preset (2g/24h/12h/3h/1h/30m/10m/5m) con stato codificato in `callback_data` (stateless), bottone "Personalizzati" che apre una `ConversationHandler` con `ForceReply` per un input libero tipo `2g,12h,10m`. Decisioni in **ADR 0015**. Nessun setting BotFather aggiunto (bottoni = puro PTB). Bug "Salva sovrascrive i custom" fixato in ADR 0019 (vedi "Roadmap verso la v1.0" sotto), con un limite residuo noto e accettato.

**Roadmap verso la v1.0 (obiettivo di Matteo: rilascio in prod entro l'inizio del campionato, 22 ago 2026):**

1. ✅ **Revisione testi e formatting dei messaggi** (PR #25, #26): passata di riscrittura in voce goliardico-fantacalcistica (ADR 0018) + giro di feedback su comandi legacy/promossi, liste bullet, scaling di `format_remaining` oltre 7 giorni (Amendment 2026-07-10 alla stessa ADR).
2. ✅ **"Ho schierato" — silenziare i reminder di una giornata già schierata** (promossa nella roadmap v1.0 il 2026-07-14, era un'idea v2.0 non pianificata; pianificata e implementata il 2026-07-14 stesso — **ADR 0021**). Solo chat private (vedi ADR 0021 per il perché: una subscription di gruppo/canale è condivisa da più manager distinti, quindi un click solo non può silenziare correttamente i reminder di tutti — servirebbe un roster per-utente, vedi "Roadmap verso la v1.1" sotto). `/ho_schierato` (nessun argomento, conferma sempre la prossima scadenza) + bottone "✅ Ho schierato" su ogni reminder inviato in privato, entrambi annullabili con "↩️ Annulla conferma". Nuova tabella `lineup_confirmations (chat_id, round)`, letta da `reschedule_reminders`/`send_reminder_job` (`reminders/jobs.py`) esattamente come `sent_reminders`.
3. **Giro di test manuali strutturati sul bot dev** — sessione dedicata a scovare bug/comportamenti non voluti e annotare nice-to-have, da eseguire **dopo** che "Ho schierato" (punto 2, versione privata) è implementata. Emersi finora (2026-07-08, durante i test del provider mock):
   - ✅ **`/personalizza_orari` → "Personalizzati" → Salva sovrascrive i custom** (fix in **ADR 0019**, 2026-07-11): dopo un salvataggio riuscito via input libero, `receive_custom_offsets` non ripristina più la griglia originale in stato interattivo (mask lossy, rischio di overwrite silenzioso a un Salva successivo) — la elimina, coerente con come il prompt `ForceReply` viene già eliminato su back/annulla/timeout. "⬅️ Indietro"/`/annulla`/timeout restano invariati (ripristinano la griglia con la mask originale, sempre accurata perché nulla è cambiato). Il difetto minore del testo stale (`_restore_grid` aggiornava solo la tastiera) è risolto di conseguenza, dato che il messaggio non esiste più.
   - ✅ **Limite residuo chiuso (ADR 0020, 2026-07-13)**: il caso lasciato aperto da ADR 0019 Decision 3 (apertura fresca di `/personalizza_orari` con offset custom già salvati → Salva su preset scartava i custom) è risolto rendendo `save_offsets_callback` merge-safe — legge la subscription dal DB, preserva gli offset non-preset e li unisce ai preset spuntati. Tradeoff accettato: la griglia non può più *rimuovere* un custom (serve "Predefiniti" o un nuovo "Personalizzati"). Nuovo helper puro `keyboards.non_preset_seconds` con test dedicati.
4. **Tag `v1.0.0`** quando il giro di test è concluso e non emergono altri bug bloccanti — è anche il momento di eseguire la **Fase 2 di ADR 0017**: cut del branch `release-1.0`, protezione, aggiunta a `ci.yml`.

**Decisione 2026-07-14**: con margine sufficiente prima del 22 agosto, Matteo ha deciso di anticipare "Ho schierato" nella v1.0 invece di lasciarla per dopo — è piccola e autocontenuta, non tocca le aree fragili appena sistemate (griglia orari), e alza la qualità percepita al lancio. Valutate e scartate per la v1.0 (restano dopo, vedi sotto): canali user-owned (4 decisioni di design aperte, merita un ADR e una sessione propria) e dashboard/pannello (rischia di riaprire la classe di bug della griglia orari appena chiusa).

**Aggiornamento 2026-07-14 (stessa giornata, prima revisione)**: Matteo aveva deciso di portare in v1.0 anche la variante gruppi di "Ho schierato" (roster-lite), inizialmente rimandata a dopo la 1.0 nella decisione precedente — vincolo non il tempo ma la **complessità del design** (roster per-utente, UI di conteggio, policy per chi lascia il gruppo, limite intrinseco del roster "lazy" da accettare esplicitamente).

**Aggiornamento 2026-07-14 (stessa giornata, seconda revisione — decisione finale per questa sessione)**: dopo aver chiuso il primo bivio di design (iscrizione al roster via bottone di auto-registrazione, non username digitati — vedi "Roadmap verso la v1.1" sotto) sono emerse altre implicazioni ancora aperte (chiusura iscrizioni, gestione di chi lascia il gruppo). Matteo ha deciso di **non forzarla nella v1.0**: si procede al rilascio 1.0 con "Ho schierato" solo in chat private (già completo, testato, ADR 0021 chiusa), e la variante gruppi diventa la prima cosa pianificata per la **v1.1**, subito dopo il tag `v1.0.0`. Tutto il lavoro di design già fatto (bivio chiuso + bivi ancora aperti) è preservato nella sezione "Roadmap verso la v1.1" sotto, non perso.

✅ **Promemoria nei topic dei gruppi forum** (2026-08-28, target `v1.2.0`): nei supergruppi con i topic attivi, `/promemoria_on`/`/personalizza_orari` (comando o bottone) eseguiti dentro un topic legano lì la consegna dei promemoria di quella chat; ripetere il comando in un topic diverso sposta il binding. Nessun comando nuovo, nessun nome di topic salvato (limite del Bot API, verificato). Decisioni in **ADR 0025**.

## Roadmap verso la v1.1 (dopo il rilascio 1.0)

🔜 **"Ho schierato" nei gruppi (roster-lite)** — prima feature pianificata dopo il tag `v1.0.0`, target v1.1 (spostata fuori dalla v1.0 il 2026-07-14: scope materialmente più grande della versione privata, con implicazioni di design ancora aperte che avrebbero rischiato la data del 22 agosto). **Da pianificare in una nuova sessione dedicata, ADR-first, dopo il tag `v1.0.0`** — non prima, per non rimettere in gioco la data di rilascio. Estende "Ho schierato" (privata, ADR 0021) ai gruppi: una subscription di gruppo è condivisa da più manager distinti, quindi serve un roster di chi conta come partecipante prima di poter silenziare correttamente. Unico approccio realistico individuato finora (l'API Telegram non espone il roster membri, solo gli admin sono enumerabili): un roster **"lazy"** — nuova tabella `group_participants (chat_id, user_id)`; conferme tracciate per `(chat_id, round, user_id)`, non più solo `(chat_id, round)`; i reminder del gruppo si silenziano solo quando *tutti i partecipanti già visti* hanno confermato per quel round; UI con un conteggio tipo "3/5 confermati". **Limite intrinseco, non risolvibile con l'API Telegram attuale**: chi non si registra mai non entra nel roster, quindi "tutti confermato" resta un'approssimazione anche a implementazione fatta — da validare esplicitamente con Matteo come limite accettabile, non un bug da risolvere.

- **Bivio #1 chiuso (2026-07-14, discusso in chat): iscrizione al roster con auto-registrazione a bottone, non digitando @username.** Scartata l'idea iniziale di far scrivere all'admin la lista degli `@username` dei giocatori: Telegram non garantisce che un bot possa risolvere uno `@username` in un `user_id` se quella persona non ha mai interagito col bot, e non tutti hanno uno username pubblico — l'idea si rompe esattamente per le persone che l'admin vorrebbe registrare. Deciso invece: il bot posta un messaggio con un bottone "Sono un manager"; ogni giocatore si auto-registra con un tap (risolve sempre `user_id` via `CallbackQuery.from_user`, zero problemi di risoluzione); l'admin lancia un comando (es. `/chiudi_iscrizioni`) quando il roster è al completo, così "tutti confermato" ha un significato preciso invece di dipendere da chi ha già cliccato "Ho schierato" almeno una volta. Se l'admin non fa nulla, resta il fallback zero-setup: il roster si popola comunque ai primi click su "Ho schierato".
  - **Implicazione da chiudere nella sessione dedicata**: il silenziamento dovrebbe scattare solo a iscrizioni chiuse (altrimenti si rischia di silenziare prematuramente prima che tutti i veri partecipanti si siano registrati) — ma se l'admin non chiude mai esplicitamente le iscrizioni, il gruppo resta bloccato sul fallback lazy per sempre? Accettabile, o serve una chiusura automatica dopo un certo tempo/round? Non deciso.
- **Bivi di design ancora aperti, da chiudere con AskUserQuestion prima della nuova ADR** (prossima libera in `docs/adr/` dopo la 0021):
  1. Gestione di chi lascia il gruppo o smette di giocare: nessuna gestione (si accetta il limite), rimozione manuale via comando admin, o `ChatMemberHandler` per il leave event (plumbing nuovo, oggi non esiste — `grep -r ChatMemberHandler src/` non trova nulla, stesso gap già notato per i canali user-owned qui sotto)?
  2. Chi può marcare "Ho schierato" nel gruppo: chiunque (probabile, è un'autodichiarazione personale) o solo admin (diverso dal pattern ADR 0012, che gate solo le azioni di subscribe/unsubscribe a livello di *tutta* la chat)? E chi può aprire/chiudere le iscrizioni al roster — presumibilmente solo admin, da confermare.
  3. UI del conteggio: si edita il messaggio di reminder condiviso con un contatore live (rischio di race su edit concorrenti da più utenti che premono quasi insieme) o si manda un messaggio di stato separato aggiornato a ogni conferma?
  4. Reset/riapertura del roster a inizio stagione o cambio lega nello stesso gruppo: comando dedicato (es. `/riapri_iscrizioni`), o si accetta che il roster cresca indefinitamente una volta chiuso (stesso trattamento "disposable" già accettato per `sent_reminders`/`lineup_confirmations`)?

Dopo la v1.1, la prossima feature grossa restano i **canali user-owned** qui sotto (4 decisioni di design proprie, ancora tutte aperte) — l'ordine tra le due non è vincolante, ma il roster-lite ha già più lavoro di design fatto.

**Idee UX per una futura v2.0 (valutate 2026-07-07):**

- ⏭️ **Canali di lega user-owned — PROSSIMA FEATURE GROSSA DA PIANIFICARE (dopo il rilascio della v1.0)**. Un utente aggiunge il bot come admin del proprio canale Telegram (fuori dal canale ufficiale @fantaformazionireminders) per ricevere lì i promemoria della propria lega. Contesto utile per la pianificazione:
  - **Perché serve un meccanismo diverso dai comandi**: i canali non possono inviare messaggi/comandi al bot (solo i post del canale stesso), quindi `/promemoria_on` è strutturalmente inutilizzabile lì. L'unico segnale disponibile è l'evento Telegram `my_chat_member` (il bot viene promosso/rimosso da admin del canale).
  - **Plumbing già pronto, niente schema da toccare**: `Subscription.chat_type` già accetta `"channel"` (è il tipo usato dalla subscription env-seeded, vedi `models.py` e i test in `test_repository.py` con `ENV_CHANNEL`); l'`origin='user'` già protegge dal pruning di `prune_channel_subscriptions` — l'amendment ADR 0008 lo dice esplicitamente: *"user rows — including channels users will add the bot to — are never touched by config changes"*. Quindi: creare la subscription è un `upsert_subscription(..., chat_type="channel", origin="user")` come già fa `subscribe_command` in `telegram/commands.py`, nessuna nuova colonna/tabella.
  - **Cosa manca**: nessun handler di questo tipo esiste oggi (`grep -r ChatMemberHandler src/` non trova nulla). Serve un `ChatMemberHandler(callback, ChatMemberHandler.MY_CHAT_MEMBER)` registrato in `app.py`, che legge `update.my_chat_member` (`ChatMemberUpdated`: `old_chat_member`/`new_chat_member`, con `.status`) per distinguere promozione ad admin (→ subscribe) da rimozione/demozione (→ unsubscribe, riusando `delete_user_subscription`).
  - **Setting BotFather da attivare**: "Channel Admin Rights", limitato a *Post Messages* (nessun altro permesso serve).
  - **Decisioni aperte da prendere in ADR** (non ancora scelte):
    1. Iscrizione automatica alla promozione ad admin, o richiedere una conferma esplicita del proprietario (e come, dato che il canale non può mandare comandi — forse un comando in privato tipo `/conferma_canale`)?
    2. Offset di default: riusare `settings.reminder_offsets` come per gruppi/privato, presumibilmente sì.
    3. Come comunicare all'owner che l'iscrizione è avvenuta/revocata, visto che il bot non può scrivere nel canale se non ha (o ha perso) i permessi di post — probabilmente un messaggio privato all'utente che ha effettuato il cambio membership (`update.my_chat_member.from_user`), sul modello di come `errors.py` scrive già su `DEBUG_CHAT_ID`.
    4. `/personalizza_orari` e `/promemoria` restano non invocabili nel canale per lo stesso motivo (niente comandi) — vanno quindi lasciati eseguibili solo in privato/gruppo, oppure serve un modo per l'owner di gestirli comunque (es. comandi in privato che operano sul canale specificando l'id)? Da decidere se è nello scope o rimandato.
- ✅ **Inline keyboard (bottoni callback)** — vedi punto 4 sopra, ADR 0015.
- **Dashboard / pannello di controllo (esplorazione futura, FUORI dalla 1.0, NON pianificata — valutata 2026-07-08)**: un comando-hub (es. `/menu`, o `/start` come home) che in un unico messaggio mostra info utili (orari dei reminder, stato iscrizione) e bottoni non 1:1 coi comandi ("Prossima scadenza", "Promemoria: ON/OFF" come toggle, "Personalizza orari", ecc.). Analisi: ha senso come UX (discoverability, un solo modello mentale, comodo su mobile) ma è **soprattutto consolidazione di superfici già esistenti** (`/start` + `/promemoria` + `/personalizza_orari` si sovrappongono), non funzione nuova; aggiunge complessità di navigazione (sotto-viste, "torna al pannello", edit-in-place) che **amplifica proprio la classe di bug della griglia orari**. Direzione allineata al fix del bug ("stato di riposo = vista di sola lettura; l'editor è una sotto-vista transitoria"). Tenuta come possibile esplorazione post-1.0, non committata a una versione; richiederà ADR + design pass propri, preferibilmente **dopo** la revisione testi e il fix display-orari della 1.0 (per non rifare il lavoro due volte).
- ✅ **"Ho schierato" (chat private)** — promossa nella roadmap v1.0 il 2026-07-14, implementata lo stesso giorno (**ADR 0021**, vedi "Roadmap verso la v1.0" sopra). La variante gruppi/roster-lite è ora in "Roadmap verso la v1.1" sopra, non più qui.
- **Inline Mode** (setting BotFather + `InlineQueryHandler`): `@bot` in una chat qualsiasi per condividere la card della prossima scadenza senza aggiungere il bot alla chat. Rimandata dopo i canali.
- Setting BotFather verificati e da lasciare così: **Allow Groups ON**, **Group Privacy ON** (il bot vede solo i /comandi nei gruppi — non disattivare); Admin Rights / Guard / Secretary / Guest / Bot-to-Bot / Threads non servono al caso d'uso attuale (Admin Rights per i canali va invece attivato per la feature sopra). "Restrict bot usage" solo sul bot dev (ridondante con `ALLOWED_CHAT_IDS`, ma difesa in più). Privacy Policy: oggi vale quella standard di Telegram; se il bot cresce, basterebbe una policy di tre righe (memorizziamo solo chat_id e orari).

**Operativo/monitoraggio:**
- **BotFather, su entrambi i bot (dev e prod)**: ✅ lista comandi aggiornata con `promemoria_on`/`promemoria_off`/`promemoria`/`personalizza_orari`. Scope: on/off e `personalizza_orari` visibili solo in *Direct Messages* + *Group Administrators* (Group Chats OFF: i non-admin riceverebbero solo il rifiuto); `/promemoria` visibile ovunque. Gli scope regolano solo la visibilità nel menu, l'enforcement admin è nel codice. **Superato dalla decisione sotto** (2026-07-14): `promemoria_on`/`promemoria_off` vanno tolti dal menu.
- La stagione 2026-27 inizia il **22 agosto 2026**: il primo reminder reale parte ~21 agosto. Il percorso reminder end-to-end è stato verificato **live sul bot dev** (via provider `mock`, ADR 0016) il 2026-07-08; resta da verificare un reminder "live" reale **in prod** sul canale alla prima giornata.
- Gli orari delle giornate lontane nel CSV sono placeholder (es. 00:00): si sistemano da soli col refresh giornaliero delle 02:00.
- **Da fare**: aggiungere il secret repo-level `FOOTBALL_DATA_API_KEY` su GitHub (Settings → Secrets and variables → Actions → Secrets) — senza, il provider football-data.org (ADR 0014) non è attivabile in caso di emergenza. Non fatto perché richiede una registrazione esterna, non automatizzabile da qui.
- **Da fare**: BotFather, su entrambi i bot — aggiungere `ho_schierato` alla lista comandi, scope **Direct Messages soltanto** (a differenza di `personalizza_orari`/`promemoria_on`/`off`, che funzionano anche nei gruppi: `/ho_schierato` lì non fa nulla per design — ADR 0021 — quindi non va mostrato nel menu *Group Administrators*, altrimenti l'admin lo prova e trova un vicolo cieco). Descrizione proposta (voce ADR 0018): "Silenzia i promemoria della giornata: formazione già schierata".
- **Da fare** (deciso 2026-07-14): togliere `promemoria_on` e `promemoria_off` dalla lista comandi di BotFather su entrambi i bot — sono i comandi "legacy" pre-bottoni (Amendment 2026-07-10 di ADR 0018), `/promemoria` col suo bottone toggle è ormai il percorso promosso. Solo il menu autocomplete cambia: gli handler restano invariati, i due comandi continuano a funzionare per chi li digita a memoria o li trova in `/help` (che resta l'unico posto a elencare *tutti* i comandi, come già deciso in ADR 0018). `/personalizza_orari` non è toccato: è un solo comando sia con sia senza argomenti, non ha una variante "legacy" separata da rimuovere dal menu.
- Migrazione VM a A1.Flex: **ancora out-of-capacity** (ritentato il 2026-07-07, nessuna disponibilità). Procedura passo-passo in `docs/DEPLOY.md` § "Migrating to a new VM".

**Come lavorare con Matteo (vedi anche memoria persistente):**
- Stile consultivo: **proporre opzioni con trade-off** (AskUserQuestion) prima di decisioni di design/UX/infra; non applicare default in silenzio, anche su dettagli.
- **ADR prima del codice** per ogni decisione architetturale.
- I merge delle PR li fa **lui**; commit e push come comandi separati.
- **Niente `Co-Authored-By` nei commit né footer "Generated with" nelle PR.**
- Prima di dichiarare finito: `uv run ruff check && uv run ruff format --check && uv run mypy src && uv run pytest` tutti verdi.

**Stato al momento dell'handoff (agg. 2026-07-14, fine sessione):** **prod = v0.12.2** (nessun nuovo tag pushato in questa sessione); `main` ha già #25/#26/#27; branch corrente **`feature/v1-release-prep`** (da `main`) con il lavoro descritto sotto, non ancora aperto come PR. Nessuna PR aperta al momento.

Sessione corrente (continua sullo stesso branch `feature/v1-release-prep`):
- **ADR 0020 + fix del limite residuo di ADR 0019 (Roadmap v1.0 punto 2, ora ✅)**: `save_offsets_callback` (`telegram/callbacks.py`) è ora merge-safe — legge la subscription corrente dal DB, preserva gli offset non-preset (custom) già salvati e li unisce ai preset spuntati nella griglia invece di rimpiazzare l'intera lista dalla sola mask. Nuovo helper puro `keyboards.non_preset_seconds` (con test in `tests/test_keyboards.py`). Chiude la Decision 3 di ADR 0019 (apertura fresca di `/personalizza_orari` con custom già salvati → Salva su preset non li scarta più). Tradeoff registrato in ADR 0020: la griglia non può più *rimuovere* un custom (serve "Predefiniti" o un nuovo submit "Personalizzati").
- **Passata di branding completata**: le occorrenze residue di "FantaFormazioni" (non spaziato) in `CLAUDE.md`, `docs/ARCHITECTURE.md`, `README.md`, `src/fantaformazionibot/__init__.py` sono state sostituite con "Fanta Formazioni Bot". Non toccati (identificatori tecnici, fuori scope): pacchetto Python `fantaformazionibot`, nome del repo, `@username` Telegram. Verificato con `grep -rn FantaFormazioni CLAUDE.md docs/ARCHITECTURE.md README.md src/fantaformazionibot/__init__.py` → nessun match residuo.
- **"Ho schierato" pianificata e implementata (Roadmap v1.0 punto 2, ora ✅) — ADR 0021**: discussione ADR-first via AskUserQuestion su 4 bivi (superficie input, permessi gruppo, undo, target round) fatta emergere un problema non previsto nello sketch dell'HANDOFF — le subscription sono per `chat_id`, non per utente, quindi un gruppo/canale è condiviso da più manager e una singola conferma silenzierebbe scorrettamente i reminder degli altri. Deciso con Matteo: **v1.0 solo chat private**, roster-lite per gruppi discusso più a fondo nella stessa sessione e infine fissato come target v1.1 (vedi "Roadmap verso la v1.1"). Implementato: nuova tabella `lineup_confirmations (chat_id, round)`; `Repository.mark_lineup_confirmed`/`unmark_lineup_confirmed`/`is_lineup_confirmed`; `commands.confirm_lineup`/`undo_lineup_confirmation` (core condiviso, stesso pattern di `subscribe`/`unsubscribe`/`set_offsets`); comando `/ho_schierato`; bottoni "✅ Ho schierato" (su ogni reminder privato, via `reminders/jobs.py::send_reminder_job`) e "↩️ Annulla conferma" (`telegram/callbacks.py`); `reschedule_reminders`/`send_reminder_job` filtrano su `lineup_confirmations` esattamente come già fanno su `sent_reminders`. `reminders/planner.py` resta puro/invariato.
- **Test aggiunti per i gap dello standard del progetto** (2026-07-14, su richiesta esplicita): `tests/test_keyboards.py` (roundtrip encode/decode e rifiuto prefissi per `lineup:confirm:`/`lineup:undo:`, contenuto delle due nuove keyboard) e `tests/test_repository.py` (`mark_lineup_confirmed`/`unmark_lineup_confirmed`/`is_lineup_confirmed`) — mancavano rispetto al pattern già in uso per le funzioni pure di `keyboards.py` e per la tabella `subscriptions`. Non toccati (gap pre-esistenti, non di questa sessione, coerenti con la scelta del progetto di non testare codice I/O-Telegram): `reminders/jobs.py`, `telegram/callbacks.py`, gli handler async di `telegram/commands.py`, `sent_reminders`/`matchdays` in `Repository`.
- `ruff check`/`ruff format --check`/`mypy src`/`pytest` (82 passed) verdi su questo branch.
- **Non ancora fatto in questa sessione**: aprire la PR verso `main` (Matteo fa i merge); il giro di test manuale sul bot dev (punto 3 sotto, ora da estendere con "Ho schierato"); aggiungere `ho_schierato` a BotFather (vedi sezione Operativo sopra).

**Prossimo lavoro pianificato**:

1. **PR di `feature/v1-release-prep` → `main`** (già aperta, **PR #28**), poi merge (fa Matteo) → deploy automatico del bot dev.
2. **Giro di test manuali strutturato sul bot dev** (ultimo pezzo prima della 1.0) — **richiede un redeploy del bot dev col provider `mock`** (ADR 0016) per generare una giornata fittizia ravvicinata: finora non è stato possibile verificare dal vivo né la nuova voce dei messaggi di reminder (pool "early" + template "ULTIMA CHIAMATA", PR #25/#26) né il fix del Salva (ADR 0019 + 0020, ora completo) né "Ho schierato" (ADR 0021). Checklist dettagliata preparata il 2026-07-13 (vedi messaggio di chat / PR description) — da estendere con: `/ho_schierato` in chat privata (conferma, già-confermato, annulla, reminder successivi silenziati e poi ripristinati), bottone sul messaggio di reminder, verifica che in gruppo/canale il comando risponda "non disponibile" e nessun bottone compaia sui loro reminder. Copre anche: `/start`, `/help`, `/promemoria_on`/`/promemoria_off`/`/promemoria`, `/personalizza_orari` (grid + Personalizzati + verifica del merge-safe Salva di ADR 0020), `/prossima_scadenza` con scadenza a più di 7/30 giorni.
3. **Tag `v1.0.0`** quando il giro di test è concluso e non emergono altri bug bloccanti — è anche il momento di eseguire la **Fase 2 di ADR 0017**: cut del branch `release-1.0`, protezione, aggiunta a `ci.yml`.
4. **Dopo il tag, "Ho schierato" nei gruppi (roster-lite) è la prima cosa pianificata per la v1.1** — nuova sessione dedicata, ADR-first (contesto tecnico completo, bivio già chiuso sull'iscrizione a bottone, e bivi ancora aperti nella sezione "Roadmap verso la v1.1" sopra). Spostata fuori dalla v1.0 il 2026-07-14: non una piccola estensione del lavoro appena fatto (nuova tabella `group_participants`, tracking per-utente, UI di conteggio, policy per chi lascia il gruppo, limite intrinseco del roster "lazy" da validare) e con implicazioni di design ancora aperte che avrebbero rischiato la data del 22 agosto.
5. Poi i **canali di lega user-owned** (contesto tecnico — perché serve `my_chat_member`, cosa nello schema è già pronto, cosa manca, le 4 decisioni aperte — nella sezione "Idee UX per una futura v2.0" sopra; ADR 0008 + amendment e ADR 0012 come riferimento implementativo). Stile ADR-first: AskUserQuestion sui bivi aperti prima del design, nuova ADR (prossima libera in `docs/adr/`) prima di implementare.

Non bloccanti, ereditati da sessioni precedenti: secret `FOOTBALL_DATA_API_KEY` da aggiungere su GitHub (non automatizzabile da qui); migrazione VM ad A1.Flex bloccata da mancanza di capacità Oracle (ritentata anche il 2026-07-14, ancora out-of-capacity); il **dashboard/pannello** resta un'esplorazione futura non pianificata, fuori dalla 1.0 (vedi sezione idee sopra); housekeeping — cancellare i branch locali già mergiati `feature/voice-and-tone-feedback`, `feature/voice-and-tone-v1`, `fix/custom-offsets-save-overwrite`.

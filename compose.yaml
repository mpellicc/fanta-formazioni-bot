services:
  bot:
    image: ghcr.io/mpellicc/fanta-formazioni-bot:${IMAGE_TAG:-latest}
    build: .
    env_file: .env
    environment:
      DATABASE_PATH: /data/fantaformazionibot.db
    volumes:
      - bot-data:/data
    restart: unless-stopped

volumes:
  bot-data:
